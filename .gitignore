# Local-only planning docs (repo is public — never commit these)
Docs/

# Agent worktrees
.worktrees/

# Dependencies
node_modules/

# Next.js
.next/
out/

# Production
build/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env files — API keys (LLM providers, analytics, etc.) live in Vercel env
# vars or local .env files only; never commit any variant
.env
.env.*
!.env.example

# Secrets / credentials, belt-and-suspenders
*.pem
*.key
*.p12
credentials*.json
service-account*.json

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Tests & coverage
coverage/
playwright-report/
test-results/

# Logs & caches
*.log
.turbo/
.eslintcache

# Editor/OS cruft
.idea/
.vscode/
*.swp

# Local Claude Code settings (keep shared settings tracked if added later)
.claude/settings.local.json

# Blender autosaves (source .blend files live in assets-src/)
*.blend1
*.blend2
